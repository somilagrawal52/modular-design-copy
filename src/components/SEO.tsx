"use client";

// In Next.js App Router, metadata is provided via `export const metadata` on page/layout.
// This component acts as a safe bridge if imported by any migrated component.
export default function SEO(_props: any) {
  return null;
}
